{
  "vocab_size": 10000,
  "embed_dim": 256,
  "hidden_dim": 512,
  "num_layers": 2,
  "checkpoint_path": "trained_model/chatbot_checkpoint.pth",
  "final_model_path": "trained_model/chatbot.pth",
  "epochs": 10,
  "batch_size": 32,
  "num_workers": 4
}
